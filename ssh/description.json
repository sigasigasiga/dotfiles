{
    "posix": {
        "config": {
            "path": "~/.ssh"
        },
        "ssh-agent.service": {
            "path": "$XDG_CONFIG_HOME/systemd/user/"
        }
    },

    "*": {
        "config": {
            "path": "~/.ssh"
        },
        "ssh-agent.service": null
    }
}
